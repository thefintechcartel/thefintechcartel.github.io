import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://thefintechcartel.github.io',
  // If deploying to yourusername.github.io (not a project page), base is '/'
  // If deploying to yourusername.github.io/tfc, set: base: '/tfc'
});
